# BaseSwiftUI Claude Discovery

Use root `AGENTS.md` as the canonical policy. Select the smallest
`.codex/skills/baseswiftui-*` skill and load only its conditional references.
Do not maintain a duplicate Claude-specific workflow here.
