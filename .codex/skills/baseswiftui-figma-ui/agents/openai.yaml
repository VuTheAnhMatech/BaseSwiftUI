interface:
  display_name: "BaseSwiftUI Figma UI"
  short_description: "Build fast, high-fidelity BaseSwiftUI UI from Figma"
  default_prompt: "Use $baseswiftui-figma-ui quick for one exact local View with bounded evidence escalation and a source-level fidelity gate; expand only for architecture or explicit runtime comparison."

dependencies:
  tools:
    - type: "mcp"
      value: "figma"
      description: "Official Figma MCP server"

policy:
  allow_implicit_invocation: true
