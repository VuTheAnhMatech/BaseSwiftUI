interface:
  display_name: "BaseSwiftUI Figma UI"
  short_description: "Create BaseSwiftUI UI from Figma quickly"
  default_prompt: "Use $baseswiftui-figma-ui quick for one exact local View; expand only if this Figma node crosses an architecture or validation boundary."

dependencies:
  tools:
    - type: "mcp"
      value: "figma"
      description: "Official Figma MCP server"

policy:
  allow_implicit_invocation: true
